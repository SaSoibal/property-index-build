[build]
  command = "npm run build"
  publish = "build"

[build.environment]
  NPM_FLAGS = "--force"
  CI = "false"

[plugins]
  [[plugins]]
  package = "@netlify/plugin-gatsby-cache"
  config = { cachePublic = false }
